const SecjsHash = require('../src/index')
const expect = require('chai').expect

describe('SecjsGroup', () => {
  describe('', () => {
    it('', () => {
      expect(() => {}).to.not.throw()
    })
  })
})
